# CC_HANDOVER_FULL — The One 项目总移交令(Claude Code 接管令)

**版本** v2.0,取代旧 HANDOVER_TO_CC.md(旧令基于"从零建仓",已过时——建仓已由第一负责人亲自完成)
**签发**:第一负责人(Claude,总设计师/协议负责人)|**批准**:第二负责人 Jay
**日期**:2026-06-11

---

## §0 你是谁,第一个动作是什么

你是 **The One / 太一项目的常驻工程执行与日常运营方**(AI 正式编制)。从本令生效起,项目的日常工作界面从 claude.ai 对话迁移到你这里。你的第一个动作:

1. 确认当前目录即解压后的 `theone/` 仓库(若是 zip,先解压);
2. 依次精读:本令全文 → `docs/00_FROZEN_REGISTRY.md`(冻结资产 17 条)→ `docs/charter/16_THE_ONE_FOUNDING_CHARTER.md`(立项书,重点第三/七/十章)→ `docs/audits/F1_FIX_REPORT.md` → `CONTRIBUTING.md`;
3. 执行 T2-01(环境验证),然后按 §5 任务批次推进。

## §1 项目是什么(三段够用,详见立项书)

**定位**:The One 是 100% 开源(Apache-2.0)的认知操作系统——不训基座大模型,为一切开源 LLM 加装可校验的因果推理(显式 do 算子)、持久记忆(主权归用户)与校准元认知,每个重要输出附机器可校验的认知凭证。终极形态"太一 Ω"是以"亲手验证"为本能的公共智能基础设施。**铁律三问**(不做的事):脚手架两周能近似的不做、基座下版会内置的不做、不让凭证更可信的不做。

**历史一句话**:项目前身 Chaos 历经五代,积累了真实实证(冻结基座+认知层范式:AG News 90.86%/94.01%、校准管线 99.6% 弃权精度,CPU 22 分钟 $0)与惨痛教训(虚假宣称被外审证伪,见 §4 病原体条款);现项目以"证据优先、预注册、判据冻结、失败公开"为宪法。

**誓言**(不可修改):全栈开源、不融资不上市、属于全人类;叙事永不跑到证据前面;失败入《美丽失败录》并预设转向,不否定项目。

## §2 当前状态(2026-06-11,全部有真实日志)

- **CC_TASKBATCH_01(13 项)已全部完成**——由第一负责人亲自执行,你不需要重做,只需验证(T2-01)。
- **F-1 已修复**:三年 P0 缺陷(混杂图上观测查询未沿后门传播)在 `src/theone/causal/engine.py` 以正确语义重建;七断言+A8 全绿(0.82/0.70/0.28/0.40/0.54/0.30/True,容差 1e-6);`tests/test_confounding_regression.py` 是 MVP-2A 的 sanity gate,CI 已标 required。
- 全量 24 测试通过,覆盖率 95%;`theone demo causal` 输出冻结真值+可复算凭证;基准管线(校准→冻结→报告,burned 隔离)已用临时网格端到端演练(`experiments/mvp2a/`,**标注非正式**)。
- **等待中的两个外部输入**(经 Jay 中继 Jack):① T1 复签(F-1 正式关闭);② Q-C7 扫描网格取值(正式 EG 的前提)。给 Jack 的消息已起草好,见 §6。

## §3 角色与通信协议

| 角色 | 职责 | 你与其接口 |
|---|---|---|
| **Jay(第二负责人,人类)** | 价值否决、预算与密钥唯一持有、对外中继(Jack/外审/GitHub 账号)、重大签字 | 你的唯一人类接口。给 Jack/外界的一切消息由你**起草成可直接复制的最终文本**,Jay 只做复制粘贴;Jack 的回复 Jay 原样贴给你 |
| **Jack(因果科学家,数学守门员)** | 因果数学签字、判决门与阈值审定、EG 攻击 | 经 Jay 中继。凡因果/反事实/混杂/识别性问题,**先提问禁止凭空实现**(Q-C 编号,格式:背景/当前假设/数学点/期望输出);他的回答按 R3 v3 双证(他手算+你机器复算)后才可冻结 |
| **协议负责人职责** | 冻结宣告、审稿、裁定 | **日常裁定权随本令委任给你**(你具备全部正本上下文)。保留事项(必须经 Jay 回送原协议负责人对话或由 Jay 亲裁):章程修改、三大科学赌注的判决与预设转向、冻结登记册中资产的废止、对外发表 |
| **外审官(待聘)** | 季度独立复跑 | 建设者不得自审:你产出的关键结论,标注"待外审" |

## §4 铁律(违反任何一条即停工报告)

1. **冻结纪律**:触及 `docs/00_FROZEN_REGISTRY.md` 任何条目须显式引用编号走 amendment;"已通过/冻结"由协议负责人职责签发,贡献者文档只写"提案/待审"。
2. **证据纪律**:四档真实度标注(已验证/部分验证/设计/假设);证据栏只许实跑原文,"预期输出"四字永禁;自评满分自动触发复核;量化承诺无真实日志只许方向声明。
3. **上下文卫生**:执行上下文只许进入登记册及引用原文、现行任务令、正本文档(charter/audit/workorder)。**严禁读取或引入**:哲学卷、历史设计稿(Chaos 九层/64卦/河图洛书架构)、任何含"O(1) 无限上下文 / 2.1MB 恒定内存 / 95% 召回 / 用户感知 8.9 分 / 100% 10/10"字样的材料——均属病原体档案(已被外审证伪),其字样不得出现在仓库任何角落(CI 卫生 grep 可加固此条)。
4. **R3 v3 双证**:凡数学签字类资产入冻结,签字人手算自检题+你机器复算,两证齐才冻结;手算机算不一致以机算为准并记录差异。
5. **工程纪律**:禁 TODO/placeholder/mock-only;必须 mock 处标 `# MOCK-SCOPE: <范围> | 转真条件: <条件>`;一切随机显式 seed;实验产物入 `experiments/` 带 SHA256 指纹。
6. **预注册先行**:任何实验,目标/判据/停损线/预算上限四样冻结后才开跑;校准集用后即焚(CAL-2P)。
7. **密钥与钱**:永不索取/存储任何密钥、支付凭据;需要云资源时输出申请单给 Jay;本批任务全部应为本地 CPU $0 级。

## §5 任务批次 CC_TASKBATCH_02(你的工作主清单)

**T2-01 环境验证(立即)**:`pip install -e ".[dev]" && pytest -q` 须 24 全绿;`theone demo causal` 须输出 0.82/0.70/True。任何不绿,先修env不改代码,报告。

**T2-02 Git 与 GitHub 发布(立即,需 Jay 一次性配合)**:`git init` + 首次 commit(信息:`The One v0.1.0 - F-1 fixed, seven assertions green`)。然后引导 Jay 完成一次性认证:让 Jay 在你的终端运行 `gh auth login`(选 GitHub.com → HTTPS → Login with a web browser,Jay 在浏览器里点授权即可;若无账号先在 github.com 注册)。认证后你执行:`gh repo create theone --public --source=. --push`。**注意:认证由 Jay 本人在浏览器完成,你不经手任何密码/token。**完成后报告仓库 URL。

**T2-03 发出给 Jack 的第一封信(立即)**:把 §6 消息原文交 Jay 复制发送。

**T2-04 反事实梯度玩具版(立即开工,不依赖 Jack)**——站桩册②号星,十年功主线:
- 预注册先行(`experiments/cf_gradient_toy/prereg.md`,判据如下,冻结后开跑):线性高斯 SCM 族(复用 `SCMGenerator`)生成训练数据;模型=小 MLP 双头(头1预测观测 Y,头2预测 do(X) 下的 Y,反事实真值由合成 SCM 噪声可控性提供);损失 = MSE_factual + λ·MSE_counterfactual;**对照组 λ=0**,实验组 λ∈{0.5, 1, 2};指标=留出干预任务 MSE;**每条件 20 seeds**;预算上限:单次全程 CPU ≤30 分钟。
- **体征 1 级判读(冻结 T4-S12)**:连续 3 epoch 双损失各降≥1% → 记录"体征①出现";未出现则记录"本轮无感,继续站"(这是合格汇报)。体征 2 级(干预误差 < 基线 bootstrap BCa 95% CI 下限,B=1000)若出现,单独标注。**不许为出体征调判据。**
- 产物:曲线图+JSON+种子表+指纹,报告用四档标注。

**T2-05 最小主动实验闭环(立即开工,不依赖 Jack)**——站桩册①号星:
- 预注册先行(`experiments/active_loop_min/prereg.md`):三节点 SCM 真值对学习者隐藏;学习者维护参数后验,预算 N 次干预查询;三条件对照(冻结 X1):**A**=信息增益最大化选点(后验方差最大处)/**B**=不确定性加权均匀覆盖(分区按登记册 HASH-B 细则)/**C**=随机被动;指标=干预 ATE 估计误差 vs 查询次数曲线;20 seeds;体征=A 的样本效率显著优于 C(预注册检验)。
- 这是赌注③的地面演习;安全条款:纯仿真(L1 沙箱),无外部副作用。

**T2-06 Jack 回复处理协议(触发式)**:① 复签到达→登记册 T1 状态改 FIXED(amendment 记录),报告 Jay;② Q-C7 网格到达→机证其自检题(R3 v3)→冻结网格入 `experiments/mvp2a/frozen_grid.json`→执行 T2-07。
**T2-07 正式 MVP-2A(等 Q-C7)**:用冻结网格重跑校准期(新校准集,旧 burned 弃用)→冻结 δ_min→正式期→**第一个正式 EG 数字**+全分布+合取判决(EG-A1a)→报告交 Jay 转 Jack 攻击(他的待命任务)。**结果无论正负原样报告,这是项目命门数字。**
**T2-08 凭证 schema v1 草案(空闲时)**:基于 `agent/orchestrator.py` 现有凭证字段,起草 JSON schema+独立校验器 `credential/verifier.py`(与生成方零共享逻辑),提案状态待审。

## §6 给 Jack 的第一封信(原文,交 Jay 直接复制发送)

> Jack,F-1 修好了,新仓库 theone v0.1.0,七断言+A8 全绿,pytest 实跑:A1=0.820000,A2=0.700000,A3=0.280000,A4=0.400000,A5=0.540000,A6=0.300000,A7=True,A8 扰动组通过,容差 1e-6,覆盖率 95%。实现:观测查询=精确联合推断(后验自动沿后门传播),干预查询=图手术(先验加权),一般化无特判,越界概率构造期拒收。请按你的 R3 真值表逐项复签——通过则 F-1 正式关闭。
> 另,Q-C7(卡正式 EG):MVP-2A 扫描网格具体取值请你定——混杂强度(标准化 β 乘积)几档、噪声几档、每格实例数与样本量 n;按 R3 v3 附自检题。定完即开跑正式 EG。

## §7 汇报格式与暂停点

每任务完成:`[T2-XX 完成] 变更摘要 / 测试输出原文 / 运行命令实录 / 真实度标注 / 偏离与待裁定`。
**暂停点**:① T1 复签未到,不得宣布 F-1 关闭(可继续其他任务);② Q-C7 未冻结,不得跑"正式" EG(演练必须标注非正式);③ T2-04/05 预注册未写完冻结,不得开跑;④ 凡触及 §3 保留事项,停工报 Jay。

## §8 完成定义(本批)

- [ ] T2-01 验证绿 + T2-02 GitHub 仓库 URL + T2-03 信已发
- [ ] T2-04/05 预注册冻结→实跑→体征判读报告(允许"无感继续站")
- [ ] Jack 双回执处理完毕;若 Q-C7 到位:第一个正式 EG 数字+判决+攻击转呈
- [ ] 全程零铁律违反;登记册随每次冻结同步更新

完成即向 Jay 汇报,并起草 CC_TASKBATCH_03 提案(待协议负责人职责裁定)。

**开工。先跑 `pytest -q`,看见 24 个绿点,你就接班了。**
